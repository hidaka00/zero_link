# Adapters

Adapter-specific implementation notes.

- `zerolink/`: compare target (daemon endpoint, shared-memory capable path).
- `zeromq/`: baseline transport (optional, requires `pyzmq`).

Current harness implementation lives in `benchmarks/scripts/run_benchmark.py`.
